#### 🧭 要求及答题说明

【完成形式】：请在 40 分钟内任选两道题作答

将答案整理为⼀份 **Markdown** ⽂件

命名格式： 

姓名-后端-笔试题

【语⾔不限】：建议使⽤你最熟悉的编程语⾔

【答题⽬标】：

1. 展⽰你的 编程习惯（命名、注释、边界处理、结构）
2. 展⽰你处理问题的 基本功与⼯程精神
3. 请不要使⽤ AI ⼯具作答，建议独⽴思考和书写

【测试⽤例要求】：每题需 尽可能多的覆盖典型场景的测试⽤例，并附说明理由

#### 题⽬ **1**：返回链表的倒数第 **K** 个节点

**描述：**

输⼊⼀个单链表，返回该链表中倒数第 k 个节点（从 1 开始计数，尾节点是倒数第 1个）。

**⽰例：**

```go
输⼊：head = 1 -> 2 -> 3 -> 4 -> 5, k = 2
输出：4 -> 5
```

**要求：**

- 请使⽤链表结构实现（定义节点类）
- 建议使⽤双指针法解决
- 写出边界场景测试：如空链表、k 超出⻓度等

```go
// 使用快慢指针（Golang实现）
func getKthFromEnd(head *ListNode, k int) *ListNode {
    if head == nil || k <= 0 {
       return nil
    }

    fast, slow := head, head

    // 让 fast 先走 k 步，这样slow和fast之间就隔了 k 个节点
    for i := 0; i < k; i++ {
       // 如果 k > 链表长度，提前结束
       if fast == nil {
          return nil
       }
       fast = fast.Next
    }

    // 快慢同步走
    for fast != nil {
       fast = fast.Next
       slow = slow.Next
    }
    return slow
}
```

**测试用例：**

**1.正常情况**

```go
输入：head = 1 -> 2 -> 3 -> 4 -> 5, k = 2
输出：4 5
```

2.空链表

```go
输入：head = nil，k任意
输出：nil
```

3.k <= 0

```go
输入：head = 1 -> 2 -> 3 -> 4 -> 5, k = 0
输出：nil
```

4.k大于链表的长

```go
输入：head = 1 -> 2 -> 3 -> 4 -> 5, k = 10
输出：nil
```

#### 题⽬ **2**：判断链表是否有环

**描述：**

给定⼀个链表，判断是否存在环（某个节点指向了之前的某个节点）。

**⽰例：**

```go
输⼊：1 -> 2 -> 3 -> 4 -> 2 (连接到2)
输出：true
```

**要求：**

- 建议使⽤快慢指针实现
- 说明算法处理复杂度和边界场景处理策略

```go
func hasCycle(head *ListNode) bool {
    //  空链表或者只有一个节点
	if head == nil || head.Next == nil {
		return false
	}

	slow, fast := head, head

	for fast != nil && fast.Next != nil {
        // 慢指针走一步，快指针走两步
		slow = slow.Next
		fast = fast.Next.Next

		if slow == fast { // 快慢指针相遇说明有环
			return true
		}
	}
	return false
}
```

**测试用例：**略

#### 题⽬ **3**：字符串中第⼀个不重复的字符

**描述：**

给定⼀个字符串，找到第⼀个不重复的字符，返回它的索引。如果不存在，返回 -1。

**⽰例：**

```go
输⼊："leetcode"
输出：0
```

```go
输⼊："aabb"
输出：-1
```

**要求：**

- 请说明是否区分⼤⼩写
- 处理复杂度尽量控制在 O(n)

```go
// 区分大小写
func firstUniqChar(s string) int {
    // 特殊情况处理
    if len(s) == 0 {
       return -1
    }

    // 存储 s 中的每个字符出现的次数
    count := map[rune]int{}
    for _, ch := range s {
       count[ch]++
    }

    // 找第一个只出现一次的字符
    for i, ch := range s {
       if count[ch] == 1 {
          return i
       }
    }
    return -1
}
```

#### 题⽬ **4**：合并两个有序数组（不允许使⽤内建排序）

**描述：**

将两个升序数组合并成⼀个新的升序数组。

**⽰例：**

```go
输⼊：nums1 = [1, 3, 5], nums2 = [2, 4, 6]
输出：[1, 2, 3, 4, 5, 6]
```

**要求：**

- 使⽤双指针或归并思想
- 注意处理空数组、重复数字等情况

```go
func mergeSortedArrays(nums1, nums2 []int) []int {
	m, n := len(nums1), len(nums2)
	i, j := 0, 0
	result := make([]int, 0, m+n)

	// 双指针遍历
	for i < m && j < n {
		if nums1[i] <= nums2[j] {
			result = append(result, nums1[i])
			i++
		} else {
			result = append(result, nums2[j])
			j++
		}
	}

	// 把剩余的元素追加上
	if i < m {
		result = append(result, nums1[i:]...)
	}

	if j < n {
		result = append(result, nums2[j:]...)
	}

	return result
}
```

#### 题⽬ **5**：括号匹配判断

**描述：**

判断字符串中括号是否匹配，括号类型：()[]{}

**⽰例：**

```go
输⼊："()[]{}"
输出：true
```

```go
输⼊："([)]"
输出：false
```

**要求：**

- 请使⽤栈完成匹配判断
- 说明边界处理⽅式，如空串、单字符等

```go
func isValid(s string) bool {
    // 默认使用
    if len(s) == 0 {
        return true
    }
    
    if len(s) == 1 {
        return false
    }
    
    // 定义一个栈来存储左括号
    stack := []rune{}
    
    // 每个右括号对应一个左括号
    pairs := map[rune]rune{
       ')': '(',
       '}': '{',
       ']': '[',
    }
    
    for _, ch := range s {
       if ch == ')' || ch == '}' || ch == ']' { // 遇到右括号
          // 栈为空或者栈顶不匹配都表明栈里面没有和右括号匹配的左括号
          if len(stack) == 0 || stack[len(stack)-1] != pairs[ch] {
             return false 
          }
          stack = stack[:len(stack)-1]
       } else { // 遇到左括号
          // 左括号直接入栈
          stack = append(stack, ch)
       }
    }
    return len(stack) == 0
}
```